{
  "name": "krpc-mini",
  "version": "1.0.0",
  "description": "KRPC client for weChat miniprogram, use wx.request(text base64) or cloud function",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test.bak": "jest --coverage --ci --reporters=default --reporters=jest-junit --detectOpenHandles",
    "prebuild": "prettier --write \"src/**/*.*s\"",
    "build": "tsc",
    "postbuild": "cp index.d.ts ./dist/"
  },
  "keywords": [],
  "author": "",
  "license": "",
  "dependencies": {
    "krpc-base": "^1.0.0"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^4.0.4",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
